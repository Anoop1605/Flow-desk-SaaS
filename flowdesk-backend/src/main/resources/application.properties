# ===== FlowDesk Backend — Application Properties =====

spring.application.name=flowdesk-backend

# ── Phase 1: H2 in-memory database (switch to PostgreSQL in Phase 2) ──
spring.datasource.url=jdbc:h2:mem:flowdesk
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ── JPA / Hibernate ──
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ── Server ──
server.port=8080

# ── PostgreSQL settings (uncomment for Phase 2) ──
# spring.datasource.url=jdbc:postgresql://localhost:5432/flowdesk
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.datasource.username=postgres
# spring.datasource.password=yourpassword
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=validate
